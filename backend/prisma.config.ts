import "dotenv/config";

// This file is used to configure Prisma's CLI tooling.
// It is not used by your application code.
// Ensure environment variables are loaded from .env file
export default {
  // Prisma will read DATABASE_URL from process.env automatically
  // This file just ensures dotenv loads the .env file first
};
